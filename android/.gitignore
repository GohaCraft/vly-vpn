gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
# Ключ подписи и пароли НИКОГДА не коммитятся — только через key.properties
# (локально) или CI-секреты (workflow создаёт key.properties из secrets).
key.properties
**/*.keystore
**/*.jks
